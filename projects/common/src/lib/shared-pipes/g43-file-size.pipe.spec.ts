import {G43FileSizePipe} from "./g43-file-size.pipe";

describe("FileSizePipe", () => {
    it("create an instance", () => {
        const pipe = new G43FileSizePipe();
        expect(pipe).toBeTruthy();
    });
});
